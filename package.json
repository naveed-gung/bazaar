{
  "name": "bazaar",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:web dev:api",
    "dev:web": "npm run dev --workspace frontend",
    "dev:api": "npm run dev --workspace @bazaar/backend",
    "build": "npm run build --workspace @bazaar/shared && npm run build --workspace @bazaar/backend && npm run build --workspace frontend",
    "typecheck": "npm run typecheck --workspace @bazaar/shared && npm run typecheck --workspace @bazaar/backend && tsc -p frontend/tsconfig.json --noEmit",
    "lint": "npm run lint --workspace @bazaar/backend && npm run lint --workspace frontend",
    "test": "npm run test --workspace @bazaar/backend",
    "db:bootstrap": "npm run db:bootstrap --workspace @bazaar/backend",
    "db:seed": "npm run db:seed --workspace @bazaar/backend"
  },
  "devDependencies": {
    "@netlify/functions": "^5.3.0",
    "npm-run-all": "^4.1.5",
    "sharp": "^0.34.5",
    "typescript": "^5.8.3"
  }
}
