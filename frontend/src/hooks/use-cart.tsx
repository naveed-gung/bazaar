// Re-export the cart hook from the canonical CartContext
// This file exists for import path compatibility only
export { useCart } from '@/contexts/CartContext';
export { useCart as default } from '@/contexts/CartContext';
